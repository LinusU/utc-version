{
  "name": "utc-version",
  "version": "1.1.0",
  "license": "MIT",
  "repository": "LinusU/utc-version",
  "bin": "bin.js",
  "engines": {
    "node": ">=6"
  },
  "scripts": {
    "test": "standard && node test"
  },
  "devDependencies": {
    "standard": "^12.0.1"
  }
}
