{
  "name": "utc-version",
  "version": "1.1.0",
  "license": "MIT",
  "repository": "LinusU/utc-version",
  "bin": "bin.js",
  "scripts": {
    "test": "standard && node test"
  },
  "devDependencies": {
    "standard": "^10.0.3"
  }
}
